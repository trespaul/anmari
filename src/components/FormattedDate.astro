---
interface Props {
  start: Date;
  end?: Date;
}

const { start, end } = Astro.props;

function format(date: Date) {
  return date.toLocaleDateString('en-za', {
    year: 'numeric',
    month: 'long',
    day: 'numeric',
  });
};

// TODO: include time if not '00:00'
// TODO: print only day number range if month and year the same
---

<time datetime={start.toISOString()}>
  { format(start)}{end && ` – ${format(end)}` }
</time>
