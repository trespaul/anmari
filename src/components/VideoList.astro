---
import YouTube from '@/components/widgets/YouTube.astro';

const { videos } = Astro.props;
---

<>
  { videos.map( ({ video_id, video_title }: { video_id: string, video_title: string }) =>
      <YouTube
        id={video_id}
        title={video_title}
      />
    )
  }
</>

